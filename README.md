# 🐦 Harpia V25 - Supernova Tensor Research

This repository presents the results of the Supernova simulation based on the **Elastic Gravitational Tensor Theory**. Unlike conventional approaches, Harpia V25 models stellar collapse as an event involving the **unwinding of coherence nodes**, where energy is transferred via a network in a deterministic and signed flow.

## 💎 Theoretical View: The SPHY Model
The Supernova documented here is not treated as a chaotic explosion, but as a **Gravitational Impedance Rupture**.
- **Vacuum Elasticity:** Spacetime acts as an elastic tensor that deforms under tension.
- **Coherence Node:** The star's core (Internal Black Hole) acts as an information coil.
- **Rebound Effect:** After the polar jets are fired, the central node recovers its spherical geometry, demonstrating the elastic memory of the gravitational mesh.

---

## 📂 Repository Structure and Datasets

The data generated by the simulations are structured in Parquet format for high-performance analysis.

### 📥 Datasets for Analysis and Auditing
The dataset files (`.parquet`) containing scalar telemetry, position vectors, and SHA-256 signatures are available for download:
👉 **[Link to Google Drive](https://drive.google.com/drive/folders/1gi_gOCPoCnBFn1N0TZHyqnmlHiT_seFU?usp=drive_link)**

### 🖥️ Scripts and Tools
| File | Description |
| :--- | :--- |
| `supernova_parquet.py` | Dataset generator script. Generates the physical simulation and signs frames with SHA-256. |
| `supernova_viewer.py` | 3D Visualizer (Ursina Engine). Reconstructs the animation from the Parquet dataset. |
| `supernova_analyzer.py` | Metrics analyzer. Calculates impedance, unwinding rate, and Fibonacci deviation. |

---

## 🚀 How to Run

### 1. Install Dependencies
To run the visualizers, analyzers, and process the data, you will need:
```bash
pip install pandas numpy matplotlib pyarrow ursina fastparquet
2. Running the Visualizer
After downloading the dataset from Google Drive and placing it in the codes folder, run:

Bash
python3 supernova_viewer.py
3. Analyzing Metrics
To generate the theoretical validation metrics:

Bash
python3 supernova_analyzer.py
